name: languist
description: Internationalization and localization support for Neo Tidaro
version: 0.1.0-pre-alpha.4
homepage: https://github.com/neo-tidaro/languist
publish_to: http://localhost:4000
resolution: workspace

environment:
  sdk: "3.9.0"
  flutter: ">=3.35.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  generate: true

executables:
  generate_l10n: generate_l10n
