/// The type of address.
enum AddressType {
  /// A home address.
  home,
  /// A work address.
  work,
  /// An office address.
  office,
  /// A parking lot address.
  parking,
  /// A garage address.
  garage,
  /// A station address.
  station,
  /// A facade address.
  facade,
  /// A stairs address.
  stairs,
  /// An other address.
  other,
}