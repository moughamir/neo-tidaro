enum KycLevel { none, basic, standard, enhanced }

/// Document types for verification purposes
enum VerificationDocType { id, passport }

/// Document types for general document management
enum DocumentType { cin, cine, referenceLetter, backgroundCheck }

enum VerificationStatus { unverified, pending, verified, rejected, expired }
