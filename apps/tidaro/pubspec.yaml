name: tidaro
description: "A Tidaro Flutter app."
publish_to: 'none'
resolution: workspace
version: 0.1.0-pre-alpha.2
environment:
  sdk: '3.9.0'
  flutter: '>=3.35.0'
dependencies:
  flutter:
    sdk: flutter
  
  
  # Workspace Dependencies - temporarily excluding core/shared to avoid flutter_secure_storage Linux issues
  ui_kit:
    path: ../../packages/ui_kit
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true