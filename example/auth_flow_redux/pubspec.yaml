name: auth_flow_redux
description: Enhanced auth flow example with Redux integration and role selection

publish_to: 'none'
resolution: workspace

version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  # Local packages
  shared:
    path: ../../packages/shared
  ui_kit:
    path: ../../packages/ui_kit
  core:
    path: ../../packages/core
  languist:
    path: ../../packages/languist

  # State management
  flutter_redux: ^0.10.0
  redux: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
