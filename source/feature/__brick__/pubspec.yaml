name: {{name.snakeCase()}}
description: A new Flutter feature package.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.9.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  # Add common dependencies for features here, e.g.,
  # bloc: ^8.1.0
  # flutter_bloc: ^8.1.0
  # equatable: ^2.0.5
  # get_it: ^7.2.0
  # injectable: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
