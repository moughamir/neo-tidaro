# {{name.pascalCase()}} Feature

This package contains the `{{name.pascalCase()}}` feature for the application.

## Overview

This feature is responsible for ... (briefly describe the feature's purpose).

## Structure

- `lib/src/{{name.snakeCase()}}/presentation`: Contains UI components (pages, widgets) and BLoC/Cubit for the feature.
- `lib/src/{{name.snakeCase()}}/domain`: Contains business logic (entities, use cases, repositories interfaces).
- `lib/src/{{name.snakeCase()}}/data`: Contains data sources and repository implementations.

## Usage

To use this feature, import the `{{name.snakeCase()}}.dart` file:

```dart
import 'package:{{name.snakeCase()}}/{{name.snakeCase()}}.dart';
```
