name: redux_showcase_example
description: "Redux state management showcase for Neo-Tidaro workspace"
publish_to: 'none'
resolution: workspace
version: 1.0.0+1

environment:
  sdk: '3.9.0'
  flutter: '>=3.35.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Workspace Dependencies
  shared:
    path: ../../packages/shared
  core:
    path: ../../packages/core
  languist:
    path: ../../packages/languist

flutter:
  uses-material-design: true
